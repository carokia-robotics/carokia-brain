# carokia-language

Language understanding and generation for Carokia. Provides LLM backend abstractions with concrete implementations for Ollama and Anthropic Claude, a conversation manager with history tracking, and a mock backend for testing. The `ollama` feature (enabled by default) connects to a local Ollama instance; the `claude` feature adds Anthropic API support.
